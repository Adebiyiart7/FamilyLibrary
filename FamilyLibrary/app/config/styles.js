import colors from "./colors";

const defaultStyles = {
  colors: colors,
  readingText: {
    lineHeight: 25,
    fontSize: 17
  },
  nullInfo: {
    color: colors.mediumText,
    textAlign: "center"
  }
};

export default defaultStyles;
